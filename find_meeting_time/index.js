exports.handler = async (request) => {
    const eventsList = request.items;
    // TODO implement
    // For each account
        // Get Free Chunks
            // start.day
            // end.day
    // Match free chunks
        // try to match everyone, provide best option if matching everyone not possible
    const response = {
        statusCode: 200
    };
    return response;
};
